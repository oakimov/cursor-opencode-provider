{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "rootDir": "."
  },
  "//": "Narrow on purpose: the bun:test suite is not typechecked by this project config; @types/bun is still included in the base config so editors can resolve bun:test.",
  "include": ["src", "test/opencode2-conformance.types.ts"]
}
