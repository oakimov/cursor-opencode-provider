{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "rootDir": "."
  },
  "//": "Narrow on purpose: the bun:test suite is not typechecked (it would need @types/bun and a cleanup pass). Only the type-only conformance guard is.",
  "include": ["src", "test/opencode2-conformance.types.ts"]
}
